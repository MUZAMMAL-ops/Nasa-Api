
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['apod.nasa.gov'], // Add the domains from which you want to allow images
    },
  };
  
  export default nextConfig;
  